class Missile : public Vessel, public Renderable {
public:
    // Constructor
    Missile() {
    }

    // Destructor
    ~Missile() {
    }

    // Override the render function from the Renderable base class
    void render() override {
        // make green rectangle
    }

    // 

private:
    // no sprite
};